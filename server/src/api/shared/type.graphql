directive @auth on FIELD_DEFINITION

type Payment {
  bank: String!
  creditNumber: String!
  expiryDate: String!
  cvc: Int!
}

type Location {
    coordinates: [Int]!
}

input LocationInfo {
  coordinates: [Int!]!
}